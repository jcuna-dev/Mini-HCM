# Backend Environment Variables

# Server Configuration
PORT=3001

# Firebase Configuration
# Option 1: Use Firebase Service Account JSON (for production)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Option 2: Use Project ID only (for development with emulator)
FIREBASE_PROJECT_ID=hcm-project
